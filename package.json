{
  "name": "excellence-school-project",
  "private": true,
  "version": "1.0.0",
  "description": "Root convenience scripts for running the client and server together in development",
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev": "concurrently \"npm:dev:server\" \"npm:dev:client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
